# HISTORY

## 2026-09-08
- 第一刀：資料類型選擇，依類型將 reqc="upfiles" input 由 text 轉 file 並設定 accept。
- 加入拖拉、貼上、多檔選取、去重及清空。
- 加入 FileSetClassifier、Shapefile 配套檢查及安全的檔案資訊列表。
- WMTS 保留文字輸入，驗證 HTTP(S) URL 後只收錄。
- 沿用共用 PHP 版型並啟用本機 Bootstrap 5；移除本階段未使用的地圖與解析器載入。
- 介面改為 3WA 深綠暗色系，調整原生選檔、表格、提示及鍵盤焦點對比。
- 第二刀：加入開始解析／取消按鈕、shpjs 6.2.0 背景 Worker、ZIP／配套檔解析、metadata 與安全屬性預覽，含容量限制及超時處理。
- 改為左右工作區：左側選檔與清單、右側圖台預留區與解析說明；窄螢幕改為單欄。
- 修正多欄屬性表直排：改橫向捲動與固定表頭；新增 DBF UTF-8／Big5 自動抽樣、CPG 優先及手動編碼重解析。
- 修正靜態資源長效快取導致頁面與 JS/CSS 版本混用：依內容產生版本碼，涵蓋 Worker 與其解析器依賴。
- 接上 GeoJSON、KML/KMZ、GPX、DXF、GeoTIFF 解析與 WMTS GetCapabilities，統一解析按鈕、背景取消及安全資訊預覽；補上各格式測試與固定版本依賴。
- 第三刀：接入 easymap7117 圖台，新增向量／基本 DXF／GeoTIFF 灰階／WMTS 顯示、來源 CRS 指定、圖層開關、縮放與安全點選屬性；大量資料採有界抽樣預覽。
- 解析結果新增座標診斷：Extent、各軸範圍、外框中心、跨度、座標樣本及原始 SHP Extent／CRS 宣告；數值候選提示不自動套用。

## 2026-09-09
- XML 來源已有 Unicode 替代字元時保留警告並繼續解析；其他結構錯誤仍拒絕。KML 座標超出經緯度範圍時明示來源問題並取消 WGS84 假設。
- 大型線面圖層新增預設流暢預覽（500 筆／20000 座標點），可取消勾選恢復原預覽上限；保留完整 metadata、範圍與抽樣圖徵的屬性對應。
- 簡化圖台：移除上方 CRS 選單、流暢預覽切換與縮放按鈕；維持套圖後自動縮放，恢復原有 5000 筆／200000 座標點預覽上限。未知 CRS 保留診斷並暫不套圖。
- WMTS 解析成功後改為自動勾選、直接套疊圖磚，圖層開關仍可停用。
- 解析完成後不再自動捲動頁面。
- Worker 回報載入、逐檔解析與整理結果階段；介面加入可取消的原生解析進度條。
- 圖台改為完整保留圖徵，依 250 筆或 25000 個座標點分批加入 Easymap，圖層名稱顯示逐批繪製進度。
- DXF 圖台新增 LWPOLYLINE/POLYLINE bulge 圓弧、BLOCK／INSERT 展開，支援插入點、縮放、旋轉及列／欄陣列；遞迴或過深 BLOCK 明確略過。
- 單批上傳檔案上限由 50 MB 放寬為 100 MB。
- 文字編碼下新增資料來源 CRS 選擇：僅 Shapefile、DXF、GeoJSON、GPX、GeoTIFF 顯示，預設 WGS84 EPSG:4326，提供自動、DMS、DM、臺灣 TM2、Web Mercator 與隱藏的臺電圖號選項；EPSG 值僅補足未宣告 CRS 的資料。
